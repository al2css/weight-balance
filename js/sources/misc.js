$(document).ready(function() {
    WeightAndBalance.init();

});

var WeightAndBalance = {
    init: function() {
        console.log('##### INIT #####');
        // $('.has_tipsy').tipsy({gravity: 's'}); // nw | n | ne | w | e | sw | s | se
    }

};
